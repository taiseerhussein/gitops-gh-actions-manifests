nodeapp:
  # This image tag can be automatically updated by invoking the workflow
  # included in this repository 
  image: quay.io/tkhussein/gitops-and-actions:sha-5bdd14e
  replicas: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "512Mi"
      cpu: "1"
